name: sales_task_tracker
description: A Flutter app for salespeople to track sales tasks offline.

publish_to: 'none'

version: 1.0.0+1

environment:
    sdk: '>=2.17.0 <3.0.0'

dependencies:
    flutter:
        sdk: flutter
    firebase_core: ^2.24.2
    cloud_firestore: ^4.15.0
    cupertino_icons: ^1.0.2
    sqflite: ^2.0.3
    path: ^1.8.2
    intl: ^0.17.0
    connectivity_plus: ^3.0.3
    firebase_auth: ^4.16.0

dev_dependencies:
    flutter_test:
        sdk: flutter
    flutter_lints: ^2.0.0

flutter:
    uses-material-design: true
